name: Import sanitized snapshot

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Stop until snapshot source is configured
        run: |
          echo "Sanitized snapshot source is not configured."
          exit 1
